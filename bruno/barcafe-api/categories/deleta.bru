meta {
  name: deleta
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/api/categories/{{categoryId}}/toggle
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
