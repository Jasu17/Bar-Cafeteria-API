meta {
  name: toggle
  type: http
  seq: 4
}

patch {
  url: {{baseUrl}}/api/categories/{{categoryId}}/toggle
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
