meta {
  name: delete
  type: http
  seq: 7
}

delete {
  url: {{baseUrl}}/api/subcategories/{{subcategoryId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
