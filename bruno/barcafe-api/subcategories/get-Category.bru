meta {
  name: get-Category
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/api/subcategories/category/{{categoryId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
