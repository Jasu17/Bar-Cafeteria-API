meta {
  name: get-subcategory
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/api/subcategories/{{subcategoryId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
