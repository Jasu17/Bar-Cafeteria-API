meta {
  name: get-category-id
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/api/products/category/{{categoryId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
