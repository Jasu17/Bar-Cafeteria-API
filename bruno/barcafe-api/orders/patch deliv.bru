meta {
  name: patch deliv
  type: http
  seq: 7
}

patch {
  url: {{baseUrl}}/api/orders/{{orderId}}/status
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "status": "DELIVERED"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
