meta {
  name: patch done
  type: http
  seq: 6
}

patch {
  url: {{baseUrl}}/api/orders/{{orderId}}/status
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "status": "DONE"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
