meta {
  name: patch in prep
  type: http
  seq: 5
}

patch {
  url: {{baseUrl}}/api/orders/{{orderId}}/status
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "status": "IN_PREPARATION"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
