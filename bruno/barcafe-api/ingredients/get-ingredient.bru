meta {
  name: get-ingredient
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/api/ingredients/{{ingredientId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
