meta {
  name: delete
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/api/ingredients/{{ingredientId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
